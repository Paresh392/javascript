//concatenaton of string

const name = "Paresh"
const repocont = 50

//conosole.log(name + repocount + "value"); //not better ways to concatenate and not relilable.

console.log(`helllo my name is ${name} and my repo count is ${repocount}`);